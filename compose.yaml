
services:
###> doctrine/doctrine-bundle ###
  mysql:
    image: mysql:8.0
    container_name: mysql_aquarium
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: FishManager
      MYSQL_USER: symfony
      MYSQL_PASSWORD: symfony
    ports:
      - "3308:3306"
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
